{
  "$comment": "Local tooling only. The deploy bundler uploads the function's own .ts files and not this, so imports must be fully qualified in the source rather than mapped here. Without nodeModulesDir set to none, `deno check` tries to resolve against the Vite app's node_modules one level up and fails.",
  "nodeModulesDir": "none"
}
